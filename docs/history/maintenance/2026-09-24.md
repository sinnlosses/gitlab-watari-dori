# ドキュメント棚卸し 2026-09-24

対応済み 4 件 / 持ち越し 1 件（メモリの更新。ユーザー判断で今回は見送り）。

## 持ち越し — メモリ `feedback_agent_token.md`

`~/.claude/projects/-Users-sinnlos-ghq-github-com-sinnlosses-gitlab-watari-dori/memory/feedback_agent_token.md`
に「3エージェント構成：`researcher`・`tdd-implementer`・`reviewer`（subagent_type で指定）」の
行が残っている。CLAUDE.md 側は下記のとおり削除したが、**メモリは毎セッション読み込まれるため、
このままだと同じ誤り（実在しないエージェント名）が次のセッションで再供給される**。

今回は対象外としたが、CLAUDE.md と食い違った状態なので、気になったら更新する。

## 検査結果（実行前 → 実行後）

| 検査 | 内容 | 実行前 | 実行後 |
| --- | --- | ---: | ---: |
| 1 | 索引の取りこぼし | 0 | 0 |
| 2 | 索引のリンク切れ | 0 | 0 |
| 3 | 先回りの空索引 | 0 | 0 |
| 4 | 相対リンク切れ | 0 | 0 |
| 5 | タスク運用節の形 | 0 | 0 |
| 6 | 実在しないパスの言及 | 0 | 0 |
| 7 | 置き場の逸脱 | 0 | 0 |
| 8 | 正典の二重化 | 2 | 2（いずれも正当と判断） |
| 9 | 索引の無い大きなドキュメント | 0 | 0 |
| 10 | スキル参照の不一致 | 0 | 0 |

確定群は実行前・実行後とも0件。**下記の「直したもの」2件はいずれも検査が拾わなかったズレ**で、
検査8の判定過程とユーザーからの指摘によって見つかった。

## 対応済み — 直したもの

### 1. CLAUDE.md「## チームエージェント方針」節（旧 L121-142）を削除

**検査10（スキル参照の不一致）はエージェント名を見ないため、検査では拾えなかった。**
ユーザーの指摘を受けて調べたところ、節が丸ごと機能しない状態だった。

| 記述 | 実物 |
| --- | --- |
| `researcher` | このリポジトリから使えない。定義は別プロジェクト `~/Downloads/program/scala/claude/.claude/agents/` にのみ存在 |
| `tdd-implementer` | **どこにも存在しない**。別プロジェクトにあるのは `tdd-writer` と `implementer` の2つ別々 |
| `reviewer` | 同上、別プロジェクトにのみ存在 |
| `Agent(researcher, run_in_background=true)` | `run_in_background` は Agent ツールに存在しないパラメータ（取るのは `description`/`prompt`/`subagent_type`/`model`/`isolation`） |
| `diagnose スキル` | 実在するスキル名は `diagnosing-bugs` |

このリポジトリにも `~/.claude/agents/` にもエージェント定義は1件も無く、
`Agent(subagent_type: "researcher")` は失敗する。

加えて、この節（役割別の名前付きエージェントに委譲）と「進捗管理とHandoff」の手順3
（`difficulty` と同じモデルを指定したサブエージェントに委譲）が**別々のことを指示していた**。

ユーザーの承認を得て、節を丸ごと削除し、**委譲のルールは「進捗管理とHandoff」手順3に一本化**した。
こちらは実在する汎用エージェント＋`model` 指定で成立する。`diagnose スキル` の言及も
この節の中だけだったため、削除と同時に解消した（`docs/` 側に言及は無い）。

### 2. README.md「CI/CD > ジョブ構成」の表に `renovate` を追加

検査8の判定で `README.md` と `CLAUDE.md` の CI/CD 節を見比べた際に見つけた、実物とのズレ。
`.gitlab-ci.yml` の実物は `audit` / `check` / `create-merge-requests` / `renovate` の4ジョブで、
`CLAUDE.md` も4ジョブと書いているが、`README.md` のジョブ表は3件だった。`CLAUDE.md` はこの表を
「正典」と名指ししているため、正典側が古い状態になっていた。

ユーザーの承認を得たうえで、実物に寄せて1行追加した。トリガー欄は `.gitlab-ci.yml` の
`rules`（`$CI_PIPELINE_SOURCE == "schedule" && $RENOVATE == "true"`）に合わせ、
`schedule（RENOVATE=true）` とした（承認時のプレビューは「schedule / 手動」だったが、実物には
手動トリガーのルールが無いため）。

## 対応済み — 判断した結果、そのままが正しかったもの

### 検査8: 見出し「CI/CD」が README.md, CLAUDE.md に重複

**今の姿の説明**だが、二重化ではない。`CLAUDE.md` の CI/CD 節はジョブ4件の1文要約と、
「手順は `README.md`「CI/CD」が正典」という明示的な参照で構成されている。`README.md` 側が
ジョブ表・CI/CD Variables の登録手順・スケジュール設定・Pipeline inputs の本文を持つ。
**片方が参照でもう片方が本文**なので正当。正典の寄せ替えは不要。

### 検査8: 検証/整形コマンド `pnpm check` が CLAUDE.md の複数箇所にある

契約としての定義は「## タスク運用」節の `- 検証コマンド:` 行1箇所のみで、行頭も値も正しい。
他の2箇所は役割が異なるため正当:

| 箇所 | 役割 |
| --- | --- |
| 「よく使うコマンド」節のコードブロック | 人間向けのコマンド案内（入口としての一覧） |
| 「テスト方針」節の IMPORTANT | 運用ルールの念押し |
| 「タスク運用」節の `- 検証コマンド:` 行 | **スキルが読む契約（正典）** |

値はすべて `pnpm check` で一致しており、ズレは無い。案内と念押しを消すと `CLAUDE.md` の
入口としての役割が落ちるため、そのまま残した。

## 整形・検証

| コマンド | 結果 |
| --- | --- |
| `pnpm format` | 成功（51ファイル） |
| `pnpm check` | 成功（11 test files / 186 tests passed） |

## コミット

していない（棚卸しはコミットの単位を決めない）。
